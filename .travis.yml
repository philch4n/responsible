language: node_js
node_js:
  - "5.6"
services:
  - postgresql
before_script:
  - npm install -g grunt-cli
  - npm install -g knex
  - psql -c 'create database test;' -U postgres
  - NODE_ENV=test node lib/schema.js
script:
  - grunt
  - npm run test-server
  - npm run test-client